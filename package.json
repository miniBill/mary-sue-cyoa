{
    "devDependencies": {
        "elm-review": "^2.13.4",
        "elm-test-rs": "^3.0.1-0"
    },
    "scripts": {
        "test:watch": "elm-test-rs --compiler $(which lamdera) --watch",
        "review:watch": "elm-review --compiler $(which lamdera) --watch --fix --allow-remove-files"
    },
    "license": "BSD-3-Clause"
}
